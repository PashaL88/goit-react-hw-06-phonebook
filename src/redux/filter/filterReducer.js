// import { FILTER_CONTACT } from './types';

// const FilterReducer = (store = '', action) => {
//   switch (action.type) {
//     case FILTER_CONTACT:

//     default:
//       return store;
//   }
// };
